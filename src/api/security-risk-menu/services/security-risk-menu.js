'use strict';

/**
 * security-risk-menu service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::security-risk-menu.security-risk-menu');
